var cardDetails = [
{
  id: Date.now();
  title: "Idea title";
  body: "Idea body. Don't ever play yourself. Every chance I get, I water the plants, Lion! Cloth talk.";
  star: false;
}
]
var cardDetails = document.querySelectorAll(".box")
// elementList = parentNode.querySelectorAll(selectors)

var ideas = []

//eventlisteners here







//functions here
function pushInfo(userImgURL, userNewTitle, userNewQuote) {
  currentCard = new Idea(id, title, body, star)
  id.push(newId.value)
  title.push(newTitle.value)
  body.push(newBody.value)
  showHide(cardForm, mainPost)
}
